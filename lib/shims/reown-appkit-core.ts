export function createAppKit() {
  throw new Error(
    "WalletConnect QR modal is unavailable in this WizPay build. Use an injected browser wallet, or add a working @reown/appkit distribution before enabling WalletConnect."
  );
}
